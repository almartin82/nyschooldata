# Convert numeric columns in assessment data

Converts count and percentage columns to numeric, handling suppression
markers (s, \*, etc.)

## Usage

``` r
convert_numeric_columns(df)
```

## Arguments

- df:

  Data frame with assessment data

## Value

Data frame with numeric columns converted
