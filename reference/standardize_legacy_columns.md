# Standardize column names from legacy format

Maps legacy column names to the modern format for consistent processing.

## Usage

``` r
standardize_legacy_columns(df)
```

## Arguments

- df:

  Data frame with legacy column names

## Value

Data frame with standardized column names
