# Check if cached data exists and is valid

Check if cached data exists and is valid

## Usage

``` r
cache_exists(end_year, type = "raw", max_age = 30)
```

## Arguments

- end_year:

  School year end

- type:

  File type

- max_age:

  Maximum age in days (default 30)

## Value

TRUE if valid cache exists
