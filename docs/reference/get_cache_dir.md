# Get the cache directory for nyschooldata

Returns the path to the cache directory, creating it if necessary. Uses
rappdirs for cross-platform cache location.

## Usage

``` r
get_cache_dir()
```

## Value

Path to cache directory
