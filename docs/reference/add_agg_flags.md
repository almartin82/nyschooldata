# Add aggregation level flags

Determines whether each row is state, district, or school level data and
adds NYC and charter school flags.

## Usage

``` r
add_agg_flags(df)
```

## Arguments

- df:

  Processed data frame

## Value

Data frame with aggregation flags
